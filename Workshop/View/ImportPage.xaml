<Page x:Class="Workshop.View.ImportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      DataContext="{Binding Source={StaticResource Locator},Path=ImportPage}"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Workshop.View"
      xmlns:control="clr-namespace:Workshop.Control"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="IndexPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions> 
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Left" >
                <Button Content="导入" Command="{Binding ImportCommand}" Width="120"></Button>
                <Button Content="计算" Command="{Binding GetDataCommand}" Width="120"></Button>

            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right" >
                <Button Content="导出" x:Name="SaveToButton" Command="{Binding SubmitCommand}" Width="120"></Button>

            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding OrderInfos}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"></DataGridTextColumn>
                <DataGridTextColumn Header="扫描时间" Binding="{Binding Createtime}"></DataGridTextColumn>
                <DataGridTextColumn Header="姓名" Binding="{Binding UserName}"></DataGridTextColumn>
                <DataGridTextColumn Header="工序" Binding="{Binding ProcedureName}"></DataGridTextColumn>
                <DataGridTextColumn Header="序列号" Binding="{Binding SerialNumber}"></DataGridTextColumn>
                <DataGridTextColumn Header="作业单" Binding="{Binding OrderNumber}"></DataGridTextColumn>
                <DataGridTextColumn Header="产品型号" Binding="{Binding ProductModelNumber}"></DataGridTextColumn>
                <DataGridTextColumn Header="产品描述" Binding="{Binding ProductDetail}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
